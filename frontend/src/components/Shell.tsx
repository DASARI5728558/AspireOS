import {BookOpen, BrainCircuit, ChartNoAxesCombined, Home, Settings, Sparkles} from 'lucide-react';
export function Shell({children,tab,setTab}:{children:React.ReactNode;tab:string;setTab:(x:string)=>void}){const nav=[['Overview',Home],['Updates',BookOpen],['Skills',BrainCircuit],['Analytics',ChartNoAxesCombined],['Settings',Settings]] as const;return <div className="layout"><aside><div className="brand"><Sparkles/> AspireOS</div><p className="muted">Capability & Knowledge Hub</p><nav>{nav.map(([name,Icon])=><button key={name} className={tab===name?'active':''} onClick={()=>setTab(name)}><Icon size={18}/>{name}</button>)}</nav><div className="profile"><div className="avatar">DV</div><span>Demo Learner<small>Individual stakeholder</small></span></div></aside><main>{children}</main></div>}
